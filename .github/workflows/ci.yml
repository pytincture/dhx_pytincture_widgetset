name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  package-and-contracts:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"

      - name: Set up locked dependency installer
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: "0.8.0"

      - name: Verify Python contracts and build artifacts
        run: |
          uv lock --check
          python -m unittest tests.test_security_contract
          uv build

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: tests/package-lock.json

      - name: Install exact browser test dependencies
        run: npm ci --prefix tests

      - name: Install Chromium
        run: npm exec --prefix tests -- playwright install --with-deps chromium

      - name: Run browser security proof
        run: npm test --prefix tests

      - name: Audit JavaScript dependencies
        run: npm audit --prefix tests --audit-level=low
